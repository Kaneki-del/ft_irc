#include "../includes/Server.hpp"


void Server::handlePivCommand(Client *client, std::vector<std::string>args){
  //retrive the right client and cehck first if it is a bot 
  //if boot do specific behaviour 
  //if not then send it to the target client
}
